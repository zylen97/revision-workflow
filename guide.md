# 学术论文修改工作流完整指南

一套基于 LaTeX + Claude Code 的学术论文修改框架——从收到审稿意见到提交修改稿的全流程操作手册。

> **本文件是唯一需要阅读的指南。** 按顺序执行即可完成从零到提交的全部工作。
> 本文件负责**流程和方法论**；分类表格和检查清单见 `reference/quick-reference.md`；操作细节和步骤见 `reference/workflow-guide.md`。

---

## 目录

0. [快速上手：拿到这个文件夹后怎么做](#0-快速上手拿到这个文件夹后怎么做)
1. [概览与核心理念](#1-概览与核心理念)
2. [项目初始化](#2-项目初始化)
3. [解析原始决定信 → comment-letter-clean.md](#3-解析原始决定信--comment-letter-cleanmd)
4. [意见分类与初步分析](#4-意见分类与初步分析)
5. [Cluster 分析与 revision-guide.md 填充](#5-cluster-分析与-revision-guidemd-填充)
6. [填充派生追踪文件](#6-填充派生追踪文件)
7. [闭环执行](#7-闭环执行)
8. [附录：跨审稿人引用规则](#附录跨审稿人引用规则)

---

## 0. 快速上手：拿到这个文件夹后怎么做

> **本章是操作速览。** 第一次使用看这里就够了；详细原理和方法论见后续章节。

### 前提

- 项目已有 `manuscript.tex`、`.bib` 文件、`latexmkrc`、`CLAUDE.md`、git 仓库
- `revision-workflow-guide/` 文件夹已放入项目根目录

### Phase A：初始化（你做 + Claude 做）

**你做的部分：**

```bash
# 1. 复制工具和模板到项目（Chapter 2.1 的 bash 命令，直接复制执行）
mkdir -p tools .claude/commands revision/drafts
cp revision-workflow-guide/tools/* tools/
cp revision-workflow-guide/.claude/commands/* .claude/commands/
cp revision-workflow-guide/templates/revision-guide.md revision/
cp revision-workflow-guide/templates/cluster-progress.md revision/
cp revision-workflow-guide/templates/response-progress.md revision/
cp revision-workflow-guide/templates/manuscript-changelog.md revision/
cp revision-workflow-guide/templates/response-letter.tex ./

# 2. 冻结基准
cp manuscript.tex manuscript-original.tex
git add manuscript-original.tex && git commit -m "Freeze baseline for latexdiff"

# 3. 按 Chapter 2.4 的指引，向已有 CLAUDE.md 追加修改工作流配置

# 4. 粘贴审稿意见（原始格式即可）
#    → 输出: revision/comment-letter.md

# 5. Claude 解析原始意见
#    → 输出: revision/comment-letter-clean.md
```

**Claude 做的部分（在 Claude Code 会话中）：**

> 确保 `revision/comment-letter.md`（原始决定信）已粘贴好后再执行以下步骤。

告诉 Claude：「读 `revision-workflow-guide/guide.md`，按 Chapter 3-6 初始化」。Claude 会：
- `/parse-decision-letter` 解析原始意见 → 生成 `comment-letter-clean.md` → 分类意见 → 聚类分析 → 填充 `revision-guide.md`
- 从 `revision-guide.md` 派生填充其余追踪文件 + 构建 `response-letter.tex` 骨架
- 产出：`revision/` 下 4 个追踪文件初始化完毕，`response-letter.tex` 骨架就绪

### Phase B：逐 Cluster 执行（Claude 为主，你审阅）

```
1. /general-response Editor, AE, R1, R2, R3    ← 生成概括性回复
2. 按 cluster-progress.md 的执行顺序，对每个 Cluster：
   /detailed-response R1-1                     ← 锚点回复
   /detailed-response R2-1                     ← 卫星回复
   /detailed-response R3-1                     ← 卫星回复
   → Claude 执行六步闭环（起草→改稿→填回复→更新进度→编译→提交）
3. 你审阅每个 Cluster 的回复，反馈修改意见
```

### Phase C：终审与提交（你做）

```bash
bash tools/make-diff.sh                        # 生成 tracked-changes PDF
# 按 reference/quick-reference.md 的 Pre-Submission Checklist 逐项核对
# 打包提交: response-letter.pdf + manuscript.pdf + manuscript-track-changes.pdf
```

### 分工速查

| 环节 | 你做 | Claude 做 |
|------|------|----------|
| 复制文件、冻结基准 | ✅ | |
| 配置 CLAUDE.md | ✅ | |
| 粘贴原始决定信 | ✅ | |
| 解析+规范化 | | ✅ /parse-decision-letter |
| 意见分类 + 聚类分析 | **审阅确认** | ✅ 分析+填写 |
| 填充追踪文件 | | ✅ 自动派生 |
| 逐条回复 + 改稿 | 审阅反馈 | ✅ 起草+执行 |
| 终审 + 提交 | ✅ | |

---

## 1. 概览与核心理念

### 全流程地图

```
收到决定信
  │
  ▼
Phase A: 初始化（本指南 Chapter 2-6）
  ├── 复制文件、冻结基准             [Chapter 2]
  ├── 解析原始决定信（/parse-decision-letter）[Chapter 3]
  ├── 逐条分类（类型+优先级）         [Chapter 4]
  ├── Cluster 分析 + revision-guide.md  [Chapter 5]  ← 核心智力工作 + 作战地图
  └── 填充派生追踪文件                  [Chapter 6]  ← 从 revision-guide.md 派生
  │
  ▼
Phase B: 执行（本指南 Chapter 7）
  ├── 对每个 Cluster 执行六步闭环
  │     ├── Step 1: 起草回复
  │     ├── Step 2: 修改原稿
  │     ├── Step 3: 填写回复信
  │     ├── Step 4: 更新进度
  │     ├── Step 5: 编译验证
  │     └── Step 6: Git 提交
  └── 用户审阅 → 反馈 → 修正（循环）
  │
  ▼
Phase C: 终审与提交（本指南 Chapter 8）
  ├── 预提交检查清单
  ├── 生成 tracked-changes PDF
  └── 打包提交
```

**时间预估**：Phase A 约 2-4 小时（取决于意见数量），Phase B 每个 Cluster 约 1-3 小时。

### 四条核心理念

**1. Cluster 组织**：不同审稿人经常提出重叠的关切。将相关意见归类为主题集群（Cluster），统一修改、分别回复。每个 Cluster 有一个锚点回复（Anchor）和若干卫星回复（Satellite）。

**2. 闭环执行**：每个 Cluster 严格走完六步——起草、改稿、填回复、更新进度、编译、提交。不跳步、不留半成品。

**3. 三重追踪**：三个互补文件维护完整状态：

| 文件 | 追踪什么 | 粒度 |
|------|---------|------|
| `cluster-progress.md` | Cluster 完成度 | 哪些 Cluster 已完成/进行中/待处理 |
| `response-progress.md` | 逐条回复状态 | 意见 → 回复 → 稿件修改映射 |
| `manuscript-changelog.md` | 累积稿件修改 | 改了什么、在哪里、为什么 |

**4. 科技写作纪律**：所有生成的英文文本——response letter 和 manuscript 修改——遵循四条规则：

| 规则 | 要求 | ❌ → ✅ |
|------|------|---------|
| 简单句 | 一句一个主要意思，避免从句嵌套 | "We revised the model, which was originally..." → "We revised the model. The original version..." |
| 主动语态 | 主语做动作 | "The definition was clarified" → "We clarified the definition" |
| 短句 | 目标 15-20 词，上限 25 词 | "In order to address the concern regarding the lack of clarity in the section, we made revisions." → "We revised the section to improve clarity." |
| 克制修饰 | 形容词/副词仅在传递新信息时用 | "We greatly appreciate this highly insightful comment" → "We appreciate this comment on [topic]" |

> **例外**：感谢语句允许一个描述性形容词（如 "constructive feedback"），正文论证部分严格执行。

### 前置条件

| 工具 | 用途 | 版本检查 |
|------|------|---------|
| `pdflatex` | LaTeX 编译 | `pdflatex --version` |
| `latexmk` | 自动化构建 | `latexmk --version` |
| `latexdiff` | Tracked changes | `latexdiff --version` |
| `git` | 版本控制 | `git --version` |
| Claude Code | AI 辅助撰写 | `claude --version` |

---

## 2. 项目初始化

### 2.1 目录结构搭建

> **预检**：如果 `revision/` 已存在且有旧文件（如之前的草稿、临时笔记），先清理或归档到 `revision/archive/`，避免与标准追踪文件混淆。

```bash
# 假设项目已有 manuscript.tex、CLAUDE.md、latexmkrc、git 仓库

# 1. 复制 diff 工具链
mkdir -p tools
cp revision-workflow-guide/tools/make-diff.sh tools/
cp revision-workflow-guide/tools/latexdiff-preamble.tex tools/

# 2. 复制 Claude Code Skills（修改工作流专属）
mkdir -p .claude/commands
cp revision-workflow-guide/.claude/commands/parse-decision-letter.md .claude/commands/
cp revision-workflow-guide/.claude/commands/detailed-response.md .claude/commands/
cp revision-workflow-guide/.claude/commands/general-response.md .claude/commands/

# 3. 创建修改追踪目录，复制空白追踪模板
mkdir -p revision/drafts
cp revision-workflow-guide/templates/revision-guide.md revision/
cp revision-workflow-guide/templates/cluster-progress.md revision/
cp revision-workflow-guide/templates/response-progress.md revision/
cp revision-workflow-guide/templates/manuscript-changelog.md revision/

# 4. 复制回复信模板
cp revision-workflow-guide/templates/response-letter.tex ./response-letter.tex
```

> **注意**：`comment-letter.md` 存放用户从邮件/投稿系统复制的**原始决定信**，无需手动整理格式。Claude 会调用 `/parse-decision-letter` 自动解析并生成规范化的 `comment-letter-clean.md`。

### 2.2 冻结基准文件

```bash
cp manuscript.tex manuscript-original.tex
git add manuscript-original.tex && git commit -m "Freeze baseline for latexdiff"
# 可选：设为只读防止误修改
chmod 444 manuscript-original.tex
```

**铁律**：`manuscript-original.tex` 从此刻起**永远不得修改**。它是 `latexdiff` 的基准，代表审稿人评审的版本。

### 2.3 为 latexmkrc 添加 auto-diff 钩子（推荐）

项目已有 latexmkrc，但需要扩展 `$success_cmd`，使编译 `manuscript.tex` 后自动生成 tracked-changes PDF。

找到 latexmkrc 中的 `$success_cmd` 行（通常类似 `$success_cmd = 'for f in build/*.pdf; do ...'`），替换为：

```perl
# 编译成功后复制 PDF 到根目录 + 自动生成 tracked-changes PDF
$success_cmd = 'for f in build/*.pdf; do [ -f "$f" ] && cp "$f" .; done; '
             . 'if [ "%R" = "manuscript" ] && [ -f "manuscript-original.tex" ] && [ -f "tools/latexdiff-preamble.tex" ]; then '
             .   'echo "[auto-diff] Regenerating highlighted diff..." && '
             .   'bash tools/make-diff.sh 2>&1 | tail -1; '
             . 'fi';
```

> 这是可选但推荐的。不加也能手动运行 `bash tools/make-diff.sh` 生成 diff。

### 2.4 向已有 CLAUDE.md 追加修改工作流配置

项目已有 CLAUDE.md（包含模板、编译、格式等基础配置）。现在需要将修改工作流专属内容**整合**到已有结构中。按以下指引将内容嵌入你 CLAUDE.md 的对应位置：

**① "关键规则"节 → 插入冻结规则**：

```markdown
### manuscript-original.tex 禁止修改
**`manuscript-original.tex` 是 latexdiff 的基准文件，任何情况下都不得修改！**
- 用于最终生成 tracked changes
- 所有修改只能在 `manuscript.tex` 上进行
```

**② "文件结构"节 → 补充新增文件**（在已有文件树中插入）：

```markdown
├── manuscript-original.tex     # ⚠️ FROZEN 基准 — 禁止修改！（用于 latexdiff）
├── response-letter.tex         # 审稿回复信
├── tools/                      # Diff 工具链（make-diff.sh + latexdiff-preamble.tex）
├── revision/                   # 修改追踪（策略+进度+审稿原始意见+drafts）
│   └── drafts/                 # Agent 工作流输出
└── .claude/commands/           # 项目级 skill 定义
```

**③ "编译命令"节 → 补充新文件的编译命令**：

```markdown
latexmk -pvc- -pv- response-letter.tex          # 编译回复信（一次性）
latexmk -pvc- -pv- supplemental-materials.tex  # 编译补充材料（一次性）
```

**④ 新增"修改工作流"节**（作为顶级 section，与"关键规则"、"编译命令"等平级）：

```markdown
## 修改工作流

### 上下文文件
每次新会话开始时读取以下文件：
1. `revision/comment-letter-clean.md` — 规范化审稿意见列表（由 /parse-decision-letter 生成）
2. `revision/revision-guide.md` — 修改策略指南
3. `revision/cluster-progress.md` — Cluster 级进度
4. `revision/response-progress.md` — 逐条回复状态

### Skills
- **`draft`** / **`polish`**：论文段落写作
- **`general-response`** / **`detailed-response`**：审稿回复生成

### 集群修改闭环
对每个 Cluster 执行完整闭环：
1. 生成回复草稿 → `revision/drafts/Comment_X-Y.md`
2. 修改 `manuscript.tex`
3. 将回复填入 `response-letter.tex`，更新 `\lineref{}` 为实际行号
4. 更新 `revision/response-progress.md` 和 `cluster-progress.md`
5. 编译验证
6. Git 提交
```

**⑤ 新增"Response Letter 格式规范"节**（紧接"修改工作流"之后）：

```markdown
## Response Letter 格式规范

### 格式规则
- `\response{}`、`\manuscriptquote{}`、`\lineref{}` **不可嵌套**
- `\response{}` **内部不能有空行**（会导致 `\par` 错误）
- `\response{}` 在 `\manuscriptquote{}` 之前关闭，之后重新打开
- `\lineref{}` 放在 `\manuscriptquote{}` 的**内容末尾**

### 引用格式
- **不使用 natbib 或任何引用包**，所有引用均为纯文本
- `(Author et al., Year)` 替代 `\citep{}`
- `Author et al. (Year)` 替代 `\citet{}`
- 两位作者写全名，三位及以上用 et al.

### 科技写作规范（铁律）
所有 response letter 和 manuscript 修改文本：
1. **简单主动句式** — 一句一意，"We revised X" 而非 "X was revised"
2. **逻辑清晰** — 句间因果/递进关系明确，用 "First,...Second,..." 而非 "Moreover,...Furthermore,..."
3. **短句** — 目标 15-20 词/句，上限 25 词
4. **克制修饰** — 删除不传递新信息的形容词/副词；感谢语句允许一个描述性形容词
```

---

## 3. 解析原始决定信 → 生成 comment-letter-clean.md

**目标**：将用户粘贴的原始决定信解析为规范化的意见列表，为后续分类和聚类做准备。

### 3.1 触发方式

```
/parse-decision-letter
```

### 3.2 输入输出

- **输入**：`revision/comment-letter.md`（原始决定信）
- **输出**：`revision/comment-letter-clean.md`（规范化意见列表）

### 3.3 解析规则

参见 `.claude/commands/parse-decision-letter.md` 中的详细规则。核心要点：

1. **识别结构** → 分离 Editor / AE / Reviewer
2. **删除样板** → 保留实质性内容
3. **编号处理** → 保留原始编号，未编号的按段落自动编号
4. **Major/Minor** → 审稿人已分类时使用 `RN-K`/`RN-mK`；未分类时使用连续编号 `RN-1, RN-2, ...`（不强行区分）

### 3.4 输出格式

输出的 `comment-letter-clean.md` 严格遵循 `templates/comment-letter-clean.md` 格式：
- 头部元信息（决定类型、日期、截止日期、编辑姓名）
- Editor/AE/每位 Reviewer 各有独立的 Markdown 标题
- 每条意见有 ID 前缀（`**R1-1.**`、`**R1-m1.**`）
- 初步分类工作区表格（供 Chapter 4 填写）

### 3.5 边界情况

- **审稿人引用了行号或方程号**：在 comment-letter-clean.md 中保留，后续核对是否仍然准确
- **审稿人附了标注的 PDF**：在意见后用 `> [附件: 标注PDF，涉及 Section X 的 Y 处]` 描述
- **Editor 要求引用期刊近期文献**：这是实质性要求，单独编号处理
- **非标准审稿格式**：审稿人未分类、编号不连续、无 AE、Q&A 含实质反馈等，处理方案见 `reference/quick-reference.md` 的 "Non-Standard Review Format" 部分

---

## 4. 意见分类与初步分析

**目标**：对每条意见标注类型和优先级，为 Cluster 分析做准备。

### 4.1 类型分类

每条意见标注为 **Modify**（需改原稿）、**Explain**（仅需回复信解释）或 **Supplement**（新增内容）。一条意见可能同时包含多种类型，标注主要类型即可。定义和判断标准见 `reference/quick-reference.md` 的 "Comment Type Classification"。

### 4.2 优先级分类

每条意见标注为 **Highest**（全局影响）、**High**（核心关切）、**Medium**（改善展示）或 **Low**（微小修改）。定义和典型例子见 `reference/quick-reference.md` 的 "Priority Levels"。

### 4.3 使用分类工作区

在 `revision/comment-letter-clean.md` 末尾的"初步分类工作区"表格中逐条填写：

```markdown
| ID | 类型 | 优先级 | 初步 Cluster | 备注 |
|----|------|--------|-------------|------|
| R1-1 | Modify | Highest | C1? | 核心概念定义问题，R2-1/R3-1 也提到 |
| R1-2 | Modify | Highest | C2? | 记号/符号问题，R3-4 类似 |
| R1-3 | Modify | High | C3? | 方法论透明化 |
| R2-1 | Explain | High | C1? | 与 R1-1 重叠 |
| ...  | ...    | ...     | ...  | ... |
```

### 4.4 识别跨审稿人重叠

读完**所有**审稿人意见后，标注以下信号：

- 两条意见提到**相同的方程/假设/变量/Section** → 极可能同一 Cluster
- 两条意见要求**同类修改**（如都要求补充论证） → 可能同一 Cluster
- 两条意见指向**同一底层学术关切**（虽然措辞完全不同） → 应该同一 Cluster

在"备注"列中标注这些关联（如 "与 R2-1 重叠"、"类似 R3-4"）。

---

## 5. Cluster 分析与 revision-guide.md 填充

**这是整个初始化阶段最重要的工作。** 本章的产出是 `revision/revision-guide.md`——整个修改工作的"作战地图"。分析方法论和文件填充同步进行：分析到哪里，就写到哪里。

### 5.1 填充基础信息（revision-guide.md Section 1-3）

先填与 Cluster 分析无关的基础信息，为后续分析提供行号参照：

**Section 1: 论文基本信息**
- 来源：投稿系统 + 期刊官网
- 直接替换占位符即可

**Section 2: 项目文件结构**
- 按实际项目目录修改模板中的文件树
- 替换 `[BIB_FILE]`、`[TEMPLATE]` 等

**Section 3: Manuscript 结构**
- 用以下命令快速获取行号：
  ```bash
  grep -n "\\\\section\|\\\\subsection" manuscript.tex
  ```
- 填入行号表（section + subsection + 关键方程 + 表格位置）
- 这个表在后续填写 `\lineref{}` 时极其有用

### 5.2 填充意见清单（revision-guide.md Section 4，Cluster 列待定）

从 Chapter 3-4 的分析成果导入：
- Editor/AE：写 1-2 句关键要点摘要
- 每位 Reviewer：填入表格，每条意见一行（ID + 核心问题）
- **Cluster 列暂时留空**——等 5.3-5.9 的聚类分析完成后回填

### 5.3 聚类原则

1. **先通读再聚类**：不要读完 R1 就开始分组。必须读完所有审稿人意见后才开始。
2. **按底层问题分组，不按表面措辞**：两条意见可能用完全不同的话说同一件事。
3. **验证可行性**：对每个 Cluster 问——"我能设计一套协调的修改来同时满足这里所有意见吗？"如果不能，Cluster 太宽，需要拆分。
4. **允许独立**：有些意见确实独立（如纯拼写校对），可单独成 Cluster 或归入"杂项"。
5. **矛盾意见**：如果两位审稿人的要求互相矛盾（如 R1 要求简化模型、R2 要求扩展模型），将矛盾意见放入同一 Cluster，标注"⚠️ 矛盾"。修改计划中明确取舍策略（需用户决策），回复信中正面说明选择理由。

### 5.4 聚类决策树

对于每条意见 X：

```
X 是否与已有 Cluster 的某条意见指向同一原稿位置（同一 Section/方程/表格）？
├── 是 → X 很可能属于该 Cluster
└── 否 ↓
    X 是否与已有 Cluster 的某条意见要求同一类型的修改？
    ├── 是，且修改之间有依赖或冲突 → X 属于该 Cluster
    ├── 是，但修改完全独立 → 可能是新 Cluster
    └── 否 ↓
        X 与已有 Cluster 是否共享同一底层学术关切？
        ├── 是 → X 属于该 Cluster
        └── 否 → X 开启新 Cluster
```

### 5.5 常见 Cluster 模式

聚类时可参照常见主题类别（定义不清、记号混乱、假设论证不足、缺乏验证、文献不足、展示改进、实践意义等）。完整的类别列表和典型示例见 `reference/quick-reference.md` 的 "Thematic Categories for Clustering"。

### 5.6 特殊情况：AI 使用披露

如果审稿人质疑 AI 工具使用（如指出 "formulaic text"、"AI hallucination" 等），这属于**合规性问题**，需要特殊处理：

- **独立 Cluster 或归入最高优先级 Cluster**：不要与其他普通意见混在一起
- **用户亲自确认回复策略**：这是学术诚信声明，不可由 AI 自行起草或决定
- **参考期刊 AI 政策**：查找并引用投稿期刊的官方 AI 使用政策 URL
- **正面回应**：无论是否使用了 AI，都需要正面回应，不能回避或含糊其辞
- **稿件内标注**：如期刊要求，在 Acknowledgments 或专门声明中披露 AI 工具的使用范围

> **铁律**：永远不要在未经用户明确确认的情况下，替用户起草 AI 使用声明。这是用户的学术诚信责任。

**后续流程**：用户确认 AI 披露策略后，按正常 Cluster 闭环执行——Claude 根据用户确认的策略起草回复，用户最终审定回复文本。如期刊要求稿件内标注，在 Step 2 中同步修改 Acknowledgments section。

### 5.7 Cluster 编号与命名

- **编号**：C1, C2, C3...，大致按优先级从高到低排列
- **命名**：2-5 词概括核心主题

示例：
```
C1: 核心概念定义与操作化    (Highest)
C2: 符号/记号一致性审计      (Highest)
C3: 方法论透明化             (High)
C4: 核心假设/模型论证        (High)
C5: 稳健性/敏感性分析        (High)
C6: 结果解释与理论意义       (Medium)
C7: 文献覆盖与期刊整合       (Medium)
C8: 图表展示改进             (Low)
```

### 5.8 锚点（Anchor）选择规则

每个 Cluster 必须选一条意见作为**锚点**——其回复将是最详细、最完整的。其他意见的回复（卫星回复）复用锚点的修改内容，但根据各自角度重新组织论证。

| 选择标准 | 说明 |
|---------|------|
| **最详细的** | 提供了最多具体要求的那条 |
| **最技术性的** | 指出了最底层问题的那条 |
| **覆盖面最广的** | 满足这条，其他意见自然被覆盖 |
| **平局时 → 来自最严格的审稿人** | 满足最高标准 = 满足所有标准 |

**示例**：

```
C1: 核心概念定义与操作化
├── R1-1: 明确核心概念的操作性定义，区分相近概念，解释作用渠道  ← ANCHOR
├── R2-1: 核心概念与相关概念的区别
├── R2-9: 实践中如何度量核心概念
└── R3-1: 阐述核心概念的"量化"方法

锚点 = R1-1
理由: 最详细（3个具体要求）+ 最技术性（概念区分是最底层问题）
      + 覆盖面最广（定义+区分+量化覆盖了 R2-1、R2-9、R3-1）
```

### 5.9 依赖映射与执行顺序

Cluster 之间可能存在执行依赖：

- **记号审计**（C2）应先于其他所有 Cluster → 否则后续 `\manuscriptquote{}` 引用的文本可能因记号变更而失效
- **核心定义**（C1）应先于模型/假设论证（C4）→ C4 需要引用 C1 建立的定义
- **敏感性分析**（C5）可能依赖方法论（C3）→ 参数范围取决于方法选择

**依赖映射方法**：

1. 列出所有 Cluster
2. 对每对 Cluster (A, B)：如果先做 A 后做 B，B 是否需要参考 A 的结果？
3. 如果是 → B depends on A
4. 确定拓扑排序（无环有向图）

**执行顺序**：根据拓扑排序结果确定执行顺序。排序原则和典型执行序列见 `reference/quick-reference.md` 的 "Execution Order Strategy"。

### 5.10 写入 Cluster 分析结果（revision-guide.md Section 5）

将 5.3-5.9 的分析成果写入 `revision-guide.md` Section 5。对每个 Cluster，填写完整信息块：

```markdown
### C1: 核心概念定义与操作化 (Highest)
**涉及**: R1-1, R2-1, R2-9, R3-1
**锚点回复**: R1-1（最详细+覆盖面最广）
**核心问题**: 当前核心概念定义模糊，需区分相近概念，
             并给出操作性定义和度量方法指引。
**影响 Section**: Introduction, Literature Review,
                 Methods/Model Description, Discussion/Implications
**修改计划**:
- Introduction 中给出操作性定义，区分核心概念与相近概念
- Literature Review 新增概念辨析段落
- Methods 部分解释变量选择的理论依据
- Discussion 扩展实践应用指引
**依赖**: 无（最先执行）
**负责人**: Claude
**备注**: 跨 4 位审稿人的意见，需要自含式回复
```

**填写"核心问题"的要领**：
- 不是转述审稿人原话（"审稿人要求明确定义"）
- 而是提炼底层问题（"当前定义模糊，需要操作性定义 + 概念区分 + 量化指引"）

**填写"修改计划"的要领**：
- 不是模糊的"修改 Introduction"
- 而是具体的"Introduction L101 处给出操作性定义，区分三个概念"

**完成后回填 Section 4 的 Cluster 列**：现在 Cluster 方案已确定，回到 Section 4 意见清单表格，补填每条意见的 Cluster 归属。

### 5.11 完成 revision-guide.md（Section 6-9）

填写剩余 Section：
- **Section 6: Response Letter 风格指南**——参照 CLAUDE.md 中的格式规则和科技写作四条规范（简单主动句、逻辑清晰、短句、克制修饰）
- **Section 7: 交付物**——列出最终要提交的文件
- **Section 8: 分工**——明确 Claude 和用户各自负责的任务
- **Section 9: 期刊特定要求**——从期刊官网获取格式、字数、图片等要求

**revision-guide.md 完成标志**：所有 Section 已填充，无 `[placeholder]` 残留。

---

## 6. 填充派生追踪文件

**目标**：从已完成的 `revision-guide.md` 派生其余 4 个追踪文件 + 构建 `response-letter.tex` 骨架。

> `revision-guide.md` 是"母文件"——以下所有文件的内容均从中导出，不需要重新分析。

### 6.1 填充 `revision/cluster-progress.md`

从 `revision-guide.md` 的 Section 5 **直接派生**：

**总览表**：

```markdown
| Cluster | 主题 | 优先级 | 原稿修改 | 回复状态 | 状态 | 负责人 |
|---------|------|--------|---------|---------|------|--------|
| C1 | Goodwill 定义与量化 | Highest | 0/4 | 0/4 | ⬜ | Claude |
| C2 | 记号一致性审计 | Highest | 0/3 | 0/3 | ⬜ | Claude |
| ... | ... | ... | ... | ... | ⬜ | ... |
```

- "原稿修改 N/N"：分母 = `revision-guide.md` 中该 Cluster 修改计划的条目数
- "回复状态 N/N"：分母 = 该 Cluster 涉及的 Comment 数量
- 所有状态初始为 ⬜（未开始）

**各 Cluster 详情**：从 `revision-guide.md` 复制每个 Cluster 的修改计划，转为勾选框：

```markdown
### C1: Goodwill 定义与量化 (⬜ 未开始)
- **涉及 Comments**: R1-1, R2-1, R2-9, R3-1
- **Anchor Response**: R1-1
- **原稿修改**:
  - [ ] Introduction L101: 操作性定义 + 概念区分
  - [ ] Literature Review L134: 新增操作性定义段落
  - [ ] Model Description L206: E₁ 不入 Q(t) 解释
  - [ ] Implications L681: 量化代理指标
- **回复状态**:
  - R1-1: ⬜
  - R2-1: ⬜
  - R2-9: ⬜
  - R3-1: ⬜
- **依赖**: 无
```

**执行顺序**：从 5.9 的排序结果直接填入。

### 6.2 填充 `revision/response-progress.md`

从 `revision-guide.md` 的 Section 4 **直接派生**：

**General Responses 表**（Editor + AE + 各 Reviewer 的总体评价）：

```markdown
| ID | 对象 | Cluster | 状态 | tex 行号 | 草稿文件 | 备注 |
|----|------|---------|------|---------|---------|------|
| Editor | Editor | — | ⬜ | [TBD] | drafts/Response_Editor.md | |
| AE | Associate Editor | — | ⬜ | [TBD] | drafts/Response_AE.md | |
| R1-0 | Reviewer #1 General | — | ⬜ | [TBD] | — | |
| R2-0 | Reviewer #2 General | — | ⬜ | [TBD] | — | |
```

**每位 Reviewer 的详细表**：

```markdown
## Reviewer #1 — Major Comments（8 条）

| ID | 简述 | Cluster | 状态 | tex 行号 | 草稿文件 | 锚点? | 交叉引用 |
|----|------|---------|------|---------|---------|-------|---------|
| R1-1 | Goodwill 定义 | C1 | ⬜ | [TBD] | drafts/Comment_R1-1.md | C1 Anchor | → R2-1, R2-9, R3-1 |
| R1-2 | 记号一致性 | C2 | ⬜ | [TBD] | drafts/Comment_R1-2.md | C2 Anchor | → R1-m2, R3-4 |
```

**交叉引用列的填写规则**：

| 情况 | 填写内容 |
|------|---------|
| 锚点回复 | `C1 Anchor → R2-1, R3-1`（列出同 Cluster 的卫星） |
| 同审稿人卫星 | `同 R1 → R1-2`（可直接引用） |
| 跨审稿人卫星 | `跨 R 自含式`（必须自含式回复） |

### 6.3 填充 `revision/manuscript-changelog.md`

初始状态最简单：

1. 填写基准信息：
   ```markdown
   > 基准文件: manuscript-original.tex — **禁止修改**
   > 工作文件: manuscript.tex
   > 最后更新: [今天日期] | 总修改数: 0
   ```

2. 行号偏移表的第一行：
   ```markdown
   | 日期 | 批次 | 净变化 | 总行数 |
   |------|------|--------|--------|
   | [DATE] | 初始基准 | — | [wc -l manuscript.tex 的结果] |
   ```

3. 修改日志留空——等 Phase B 执行阶段逐步填写

### 6.4 构建 `response-letter.tex` 骨架

从模板出发，完成以下替换：

1. 标题中的 `[MANUSCRIPT-ID]` → 实际稿件编号
2. 开篇段落的 `[开篇段落: ...]` → 感谢 + 修改概述（可用 `/general-response` 生成）
3. 对 Editor → 粘贴 Editor 意见原文
4. 对 AE → 粘贴 AE 意见原文
5. 对每位 Reviewer：
   - 粘贴 General Assessment 原文
   - 对每条 Major/Minor Comment：
     - `\reviewercomment{Comment #N-K. [简要主题]}`
     - 粘贴意见原文
     - `\responseheader`
     - `\response{[TO BE FILLED]}`
     - `\bigskip`
6. 更新目录中的超链接（如有更多/更少 Reviewer）

**验证**：
```bash
grep -c "TO BE FILLED" response-letter.tex
```
这个数字应等于 `response-progress.md` 中需要填写的总条目数。

### 6.5 初始化完成检查

完成 Chapter 2-6 后，按 `reference/quick-reference.md` 的 "Cluster Analysis Checklist" 逐项验证（comment-letter-clean 已生成、revision-guide 填充、追踪文件初始化、response-letter 骨架、manuscript-original 存在、编译通过、Git commit）。

**通过检查 → Phase A 完成，可以进入 Phase B 执行阶段。**

---

## 7. 闭环执行

### 7.1 General Responses（先处理）

在开始 Cluster 闭环之前，先生成 Editor、AE 和各 Reviewer 的概括性回复：

```
/general-response Editor, AE, R1, R2, R3
```

> 如无 AE 则省略。审稿人编号按实际情况调整。

这些回复简短（2-3 句），表达感谢并概述修改方向。不需要 `\manuscriptquote{}` 和 `\lineref{}`。

填入 `response-letter.tex` 对应位置，更新 `response-progress.md` 状态。

### 7.2 六步闭环（每个 Cluster）

按 `cluster-progress.md` 中的执行顺序，对每个 Cluster 严格执行六步：

1. **起草回复** — 对每条意见调用 `/detailed-response`（锚点 → 卫星），输出到 `revision/drafts/`
2. **修改原稿** — 将草稿 Part 3 建议应用到 `manuscript.tex`（如涉及补充材料，同步修改 `supplemental-materials.tex`），整个 Cluster 批量完成
3. **填写回复信** — 将草稿 Part 2 粘贴到 `response-letter.tex`，替换占位符，更新行号
4. **更新进度** — 更新 `cluster-progress.md`（开始处理时标记 🔶，完成后标记 ✅）、`response-progress.md`、`manuscript-changelog.md`
5. **编译验证** — `latexmk manuscript.tex` + `latexmk -pvc- -pv- response-letter.tex`（如修改了补充材料：`+ latexmk -pvc- -pv- supplemental-materials.tex`）
6. **Git 提交** — `git commit -m "Complete cluster C[N]: [theme]"`

> 每步的详细操作流程见 `reference/workflow-guide.md`；清单式速查见 `reference/quick-reference.md` 的 "Six-Step Closed Loop"。

> **用户操作阻断**：如果某个 Cluster 的修改需要用户执行非文字工作（如跑 MATLAB、生成新图表、补充实验数据），处理方式：
> 1. Claude 先完成该 Cluster 的 Step 1（起草回复），在 Part 3 中标注"**需要用户操作**"的修改项
> 2. 跳过 Step 2-3 中用户操作相关的部分，先处理其他可完成的 Cluster
> 3. 用户完成操作后，回到该 Cluster 继续 Step 2-6
> 4. 在 `cluster-progress.md` 中标记为 `🔶 等待用户操作`

### 7.3 用户审阅与反馈

AI 生成的回复是起点。每个 Cluster 完成后建议用户审阅：

- **直接反馈**：告诉 Claude 需要修改什么
- **结构化反馈**：创建 `revision/issue.md`，逐条列出修改意见

**锚点修改后的交叉一致性**：如果用户修改了锚点回复，同 Cluster 的所有卫星回复必须检查一致性。详细的反馈处理流程和终止条件见 `reference/workflow-guide.md`。

---

---

## 附录：跨审稿人引用规则

**铁律**：审稿人只能看到回复信中属于自己的部分。编辑系统只向每位审稿人发送写给他们的回复。

| 场景 | 允许？ | 示例 |
|------|--------|------|
| R1-4 引用 R1-3 | **是** | "As discussed in our response to Comment #1-3 above..." |
| R1-4 引用 R2-1 | **否** | "As explained in our response to Reviewer #2..." |
| R1-4 和 R2-1 引用相同的稿件修改 | **是** | 两者都可以使用相同的 `\manuscriptquote{}` 和 `\lineref{}` |

**自含式回复原则**：跨审稿人的卫星回复必须包含完整论证，让该审稿人独立满意，并根据其**具体提问角度**重新组织。详细的自含式回复要求见 `.claude/commands/detailed-response.md` 的"论证策略"部分。

---

## 文件关系

| 文件 | 定位 | 什么时候看 |
|------|------|-----------|
| **本文件** (`guide.md`) | 流程 + 方法论 + 决策框架 | 开始新修改项目时，从头到尾跟着做 |
| `reference/quick-reference.md` | 分类表格 + 检查清单 + 速查卡 | 分类意见、核对进度、预提交检查 |
| `reference/workflow-guide.md` | 操作手册 + 详细步骤 + 恢复流程 | 闭环执行每一步的具体操作 |

---

## 目录结构

```
revision-workflow-guide/
├── guide.md                     # 本文件（主指南）
│
├── tools/                       # Diff 工具链
│   ├── make-diff.sh             # 生成 tracked-changes PDF
│   └── latexdiff-preamble.tex   # Diff 样式定义
│
├── templates/                   # 追踪文件模板
│   ├── comment-letter.md        # 原始决定信模板（用户粘贴）
│   ├── comment-letter-clean.md  # 规范化意见列表模板（/parse-decision-letter 输出）
│   ├── revision-guide.md        # 修改策略模板
│   ├── cluster-progress.md      # Cluster 进度追踪模板
│   ├── response-progress.md     # 回复进度追踪模板
│   ├── manuscript-changelog.md  # 修改日志模板
│   └── response-letter.tex      # 回复信 LaTeX 模板
│
├── .claude/commands/            # Claude Code Skills
│   ├── parse-decision-letter.md # /parse-decision-letter — 原始决定信解析
│   ├── detailed-response.md     # /detailed-response — 逐条详细回复
│   └── general-response.md      # /general-response — 概括性回复
│
└── reference/                   # 参考文档
    ├── workflow-guide.md        # 执行阶段操作参考
    └── quick-reference.md       # 速查卡和检查清单
```
